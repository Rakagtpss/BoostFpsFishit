local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")

local Player = Players.LocalPlayer
local Char = Player.Character or Player.CharacterAdded:Wait()
local HRP = Char:WaitForChild("HumanoidRootPart")

-- // State
local AfkModeActive = false 
local Optimized = false
local Minimized = false
local WowEnabled = false
local BV, Conn

-- // Event State
local availableEvents = {} -- Menyimpan Instance (Part/Model) langsung
local currentEventIndex = 1

-- // FPS Settings
local fpsLimits = {30, 60, 144, 999} 
local currentLimitIndex = 4 

-- // UI Config (Dark Wide)
local UI_WIDTH = 260
local UI_HEIGHT = 260
local ACCENT = Color3.fromRGB(0, 255, 170)
local BG_DARK = Color3.fromRGB(15, 15, 17)
local ITEM_BG = Color3.fromRGB(25, 25, 30)

-- // Cleanup
if CoreGui:FindFirstChild("SeraphinHelper") then CoreGui.SeraphinHelper:Destroy() end

-- // UI Creation
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "SeraphinHelper"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.3, 0)
MainFrame.BackgroundColor3 = BG_DARK
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 6)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 30)
Header.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 6)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.6, 0, 1, 0)
Title.Position = UDim2.new(0.04, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">HELPER</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(0.9,0.9,0.9)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 40, 0, 18)
MinBtn.Position = UDim2.new(1, -45, 0.5, -9)
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 12
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 4)

-- Scrolling Content
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -10, 1, -60)
ScrollFrame.Position = UDim2.new(0, 5, 0, 35)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 2
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollFrame.ScrollBarImageColor3 = ACCENT

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 5)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Helper Function UI
local function CreateButton(text)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.98, 0, 0, 32)
    btn.Text = text
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 10
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.BackgroundColor3 = ITEM_BG
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    
    btn.MouseEnter:Connect(function() btn.BackgroundColor3 = Color3.fromRGB(35, 35, 40) end)
    btn.MouseLeave:Connect(function() btn.BackgroundColor3 = ITEM_BG end)
    
    return btn
end

local function CreateLabel(text)
    local lbl = Instance.new("TextLabel", ScrollFrame)
    lbl.Size = UDim2.new(0.98, 0, 0, 20)
    lbl.Text = text
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 9
    lbl.TextColor3 = ACCENT
    lbl.BackgroundTransparency = 1
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    return lbl
end

-- // FITUR UI
CreateLabel("  MAIN FEATURES")
local PotatoBtn = CreateButton("Booster Mode: OFF")
local WowBtn = CreateButton("WALK ON WATER: OFF")
local RenderBtn = CreateButton("No Render 3D: OFF")
local FpsCapBtn = CreateButton("FPS CAP: UNLIMITED")

CreateLabel("  EVENT TELEPORT (WS Index 40)")
local ScanBtn = CreateButton("SCAN EVENTS")
local SelectEventBtn = CreateButton("Pilih: [Scan Dulu]")
local TeleportEventBtn = CreateButton("TELEPORT SEKARANG")

-- Footer
local FpsDisplay = Instance.new("TextLabel", MainFrame)
FpsDisplay.Size = UDim2.new(1, 0, 0, 20)
FpsDisplay.Position = UDim2.new(0, 0, 1, -20)
FpsDisplay.BackgroundTransparency = 1
FpsDisplay.TextColor3 = Color3.fromRGB(100, 100, 100)
FpsDisplay.Text = "FPS: -- | SERAPHIN"
FpsDisplay.Font = Enum.Font.Code
FpsDisplay.TextSize = 10

-- // LOGIC UTAMA

-- Fungsi Scan Event (Target: workspace:GetChildren()[40])
ScanBtn.MouseButton1Click:Connect(function()
    availableEvents = {}
    currentEventIndex = 1
    
    local success, err = pcall(function()
        local wsChildren = workspace:GetChildren()
        
        -- Cek apakah index ke-40 ada
        if #wsChildren >= 40 then
            local targetFolder = wsChildren[40] -- Mengambil object ke-40 sesuai request
            
            -- Memasukkan children dari object ke-40 ke dalam list
            for _, v in pairs(targetFolder:GetChildren()) do
                if v:IsA("BasePart") or v:IsA("Model") then
                    table.insert(availableEvents, v)
                end
            end
        else
            warn("Workspace tidak memiliki 40 children!")
        end
    end)
    
    if #availableEvents > 0 then
        ScanBtn.Text = "SCAN: " .. #availableEvents .. " Event Ditemukan"
        ScanBtn.TextColor3 = ACCENT
        SelectEventBtn.Text = "Pilih: " .. availableEvents[1].Name .. " (1/" .. #availableEvents .. ")"
    else
        ScanBtn.Text = "SCAN: Gagal / Kosong (Cek Index 40)"
        ScanBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
        SelectEventBtn.Text = "Pilih: [Kosong]"
    end
end)

-- Fungsi Pilih Event (Ganti Event 1 -> Event 2 dst)
SelectEventBtn.MouseButton1Click:Connect(function()
    if #availableEvents == 0 then return end
    
    currentEventIndex = currentEventIndex + 1
    if currentEventIndex > #availableEvents then
        currentEventIndex = 1
    end
    
    local currentObj = availableEvents[currentEventIndex]
    SelectEventBtn.Text = "Pilih: " .. currentObj.Name .. " (" .. currentEventIndex .. "/" .. #availableEvents .. ")"
end)

-- Fungsi Teleport
TeleportEventBtn.MouseButton1Click:Connect(function()
    if #availableEvents == 0 then 
        TeleportEventBtn.Text = "Scan Dulu!"
        task.wait(1)
        TeleportEventBtn.Text = "TELEPORT SEKARANG"
        return 
    end
    
    local targetObj = availableEvents[currentEventIndex]
    
    if targetObj then
        if targetObj:IsA("Model") and targetObj.PrimaryPart then
            HRP.CFrame = targetObj.PrimaryPart.CFrame + Vector3.new(0, 5, 0)
            TeleportEventBtn.Text = "Teleporting..."
        elseif targetObj:IsA("BasePart") then
            HRP.CFrame = targetObj.CFrame + Vector3.new(0, 5, 0)
            TeleportEventBtn.Text = "Teleporting..."
        else
            -- Fallback jika Model tidak punya PrimaryPart, coba cari child part
            local part = targetObj:FindFirstChildWhichIsA("BasePart")
            if part then
                HRP.CFrame = part.CFrame + Vector3.new(0, 5, 0)
                TeleportEventBtn.Text = "Teleporting..."
            else
                TeleportEventBtn.Text = "Event Tidak Valid!"
            end
        end
    else
        TeleportEventBtn.Text = "Event Hilang!"
    end
    
    task.wait(1)
    TeleportEventBtn.Text = "TELEPORT SEKARANG"
end)

-- Logic FPS Cap
FpsCapBtn.MouseButton1Click:Connect(function()
    currentLimitIndex = currentLimitIndex + 1
    if currentLimitIndex > #fpsLimits then currentLimitIndex = 1 end
    local newLimit = fpsLimits[currentLimitIndex]
    
    if setfpscap then setfpscap(newLimit) end
    
    FpsCapBtn.Text = (newLimit > 900) and "FPS CAP: UNLIMITED" or "FPS CAP: " .. newLimit
end)

-- Potato Mode
PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "Booster Mode: ON" or "Booster Mode: OFF"
    PotatoBtn.TextColor3 = Optimized and ACCENT or Color3.fromRGB(200, 200, 200)
    
    Lighting.GlobalShadows = not Optimized
    if Optimized then
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Material = Enum.Material.SmoothPlastic
                v.CastShadow = false
            elseif v:IsA("Texture") or v:IsA("Decal") then
                v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            end
        end
    end
end)

-- AFK Render
RenderBtn.MouseButton1Click:Connect(function()
    AfkModeActive = not AfkModeActive
    RunService:Set3dRenderingEnabled(not AfkModeActive)
    RenderBtn.Text = AfkModeActive and "No Render 3D: ON" or "No Render 3D: OFF"
    RenderBtn.TextColor3 = AfkModeActive and Color3.fromRGB(255, 50, 50) or Color3.fromRGB(200, 200, 200)
end)

-- Walk on Water
WowBtn.MouseButton1Click:Connect(function()
    WowEnabled = not WowEnabled
    WowBtn.Text = WowEnabled and "Walk On Water: ON" or "Walk On Water: OFF"
    WowBtn.TextColor3 = WowEnabled and ACCENT or Color3.fromRGB(200, 200, 200)
    
    if WowEnabled then
        BV = Instance.new("BodyVelocity", HRP)
        BV.MaxForce = Vector3.new(0, 1e6, 0)
        BV.Velocity = Vector3.zero
        Conn = RunService.Heartbeat:Connect(function()
            if HRP then HRP.AssemblyLinearVelocity = Vector3.new(HRP.AssemblyLinearVelocity.X, 0, HRP.AssemblyLinearVelocity.Z) end
        end)
    else
        if Conn then Conn:Disconnect() end
        if BV then BV:Destroy() end
    end
end)

-- Minimize Function
MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    if Minimized then
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, 30), "Out", "Quad", 0.3, true)
        ScrollFrame.Visible = false
        FpsDisplay.Visible = false
        MinBtn.Text = "+"
    else
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
        task.wait(0.2)
        ScrollFrame.Visible = true
        FpsDisplay.Visible = true
        MinBtn.Text = "-"
    end
end)

-- FPS Counter
local lastT = os.clock()
local frames = 0
RunService.RenderStepped:Connect(function()
    frames = frames + 1
    if os.clock() - lastT >= 1 then
        FpsDisplay.Text = "FPS: " .. frames .. " | SERAPHIN"
        frames = 0
        lastT = os.clock()
    end
end)
