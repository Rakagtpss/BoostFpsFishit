local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Player = Players.LocalPlayer

-- // UI SETUP
local function getUiParent()
    local playerGui = Player:FindFirstChild("PlayerGui")
    if playerGui then return playerGui end
    local success, core = pcall(function() return game:GetService("CoreGui") end)
    if success then return core end
    return nil
end

local ParentTarget = getUiParent()
if not ParentTarget then warn("Gagal menemukan tempat untuk UI!") return end

-- // Cleanup UI Lama
for _, child in pairs(ParentTarget:GetChildren()) do
    if child.Name == "SeraphinHelper" then child:Destroy() end
end

-- // UI Creation
local Gui = Instance.new("ScreenGui")
Gui.Name = "SeraphinHelper"
Gui.Parent = ParentTarget
Gui.ResetOnSpawn = false
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- // UI Config
local UI_WIDTH = 240
local UI_HEIGHT = 420 -- Dipertinggi untuk kotak input
local ACCENT = Color3.fromRGB(0, 255, 170)

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.6, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">HELPER</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 45, 0, 22)
MinBtn.Position = UDim2.new(0.75, 0, 0.5, -11)
MinBtn.Text = "HIDE"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 9
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 4)

-- Scrolling Content
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -10, 1, -60)
ScrollFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 2
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollFrame.CanvasSize = UDim2.new(0,0,0,0)

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 6)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
Layout.SortOrder = Enum.SortOrder.LayoutOrder

local function CreateButton(text, layoutOrder)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.95, 0, 0, 35)
    btn.Text = text
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 10
    btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
    btn.LayoutOrder = layoutOrder or 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- // VARIABEL STATE
local SelectedTotemName = nil
local SelectedSellItem = nil
local TotemThread = nil
local SellThread = nil
local AutoTotemEnabled = false
local AutoSellEnabled = false
local Optimized = false
local Minimized = false
local WowEnabled = false
local AfkModeActive = false
local BV, Conn
local fpsLimits = {30, 60, 144, 999}
local currentLimitIndex = 4

-- // ==========================================
-- // [SYSTEM 1] TOTEM SCANNER
-- // ==========================================

local TotemDropdownOpen = false
local TotemSelectorBtn = CreateButton("Pilih Totem: [None]", 1)
TotemSelectorBtn.TextColor3 = ACCENT

local TotemDropdownFrame = Instance.new("ScrollingFrame", ScrollFrame)
TotemDropdownFrame.Name = "TotemDropdownFrame"
TotemDropdownFrame.Size = UDim2.new(0.9, 0, 0, 0)
TotemDropdownFrame.BackgroundTransparency = 1
TotemDropdownFrame.ScrollBarThickness = 2
TotemDropdownFrame.LayoutOrder = 2
TotemDropdownFrame.Visible = false
Instance.new("UIListLayout", TotemDropdownFrame).Padding = UDim.new(0, 2)

local function RefreshTotemList()
    for _, child in pairs(TotemDropdownFrame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    local path = ReplicatedStorage:FindFirstChild("Modules")
    if path then path = path:FindFirstChild("ModelDownloader") end
    if path then path = path:FindFirstChild("Collection") end
    if path then path = path:FindFirstChild("Totems") end
    
    if not path then return 0 end

    local items = path:GetChildren()
    for _, totem in pairs(items) do
        local choiceBtn = Instance.new("TextButton", TotemDropdownFrame)
        choiceBtn.Size = UDim2.new(1, 0, 0, 25)
        choiceBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
        choiceBtn.TextColor3 = Color3.new(0.8, 0.8, 0.8)
        choiceBtn.Text = totem.Name
        choiceBtn.Font = Enum.Font.Gotham
        choiceBtn.TextSize = 10
        Instance.new("UICorner", choiceBtn).CornerRadius = UDim.new(0, 4)
        
        choiceBtn.MouseButton1Click:Connect(function()
            SelectedTotemName = totem.Name
            TotemSelectorBtn.Text = "Totem: " .. totem.Name
            TotemDropdownOpen = false
            TotemDropdownFrame.Visible = false
            TotemDropdownFrame.Size = UDim2.new(0.9, 0, 0, 0)
        end)
    end
    
    local contentHeight = #items * 27
    if contentHeight > 120 then contentHeight = 120 end
    TotemDropdownFrame.CanvasSize = UDim2.new(0, 0, 0, #items * 27)
    return contentHeight
end

TotemSelectorBtn.MouseButton1Click:Connect(function()
    TotemDropdownOpen = not TotemDropdownOpen
    if TotemDropdownOpen then
        local height = RefreshTotemList() or 100
        TotemDropdownFrame.Visible = true
        TotemDropdownFrame:TweenSize(UDim2.new(0.9, 0, 0, height), "Out", "Quad", 0.3, true)
    else
        TotemDropdownFrame:TweenSize(UDim2.new(0.9, 0, 0, 0), "Out", "Quad", 0.3, true)
        task.wait(0.3)
        TotemDropdownFrame.Visible = false
    end
end)

-- // ==========================================
-- // [SYSTEM 2] SELL SCANNER & AMOUNT INPUT
-- // ==========================================

local SellDropdownOpen = false
local SellSelectorBtn = CreateButton("Pilih Item Jual: [None]", 3)
SellSelectorBtn.TextColor3 = Color3.fromRGB(255, 100, 100)

local SellDropdownFrame = Instance.new("ScrollingFrame", ScrollFrame)
SellDropdownFrame.Name = "SellDropdownFrame"
SellDropdownFrame.Size = UDim2.new(0.9, 0, 0, 0)
SellDropdownFrame.BackgroundTransparency = 1
SellDropdownFrame.ScrollBarThickness = 2
SellDropdownFrame.LayoutOrder = 4
SellDropdownFrame.Visible = false
Instance.new("UIListLayout", SellDropdownFrame).Padding = UDim.new(0, 2)

-- --- FITUR BARU: INPUT JUMLAH ---
local AmountFrame = Instance.new("Frame", ScrollFrame)
AmountFrame.Size = UDim2.new(0.95, 0, 0, 30)
AmountFrame.BackgroundTransparency = 1
AmountFrame.LayoutOrder = 5

local AmountLabel = Instance.new("TextLabel", AmountFrame)
AmountLabel.Size = UDim2.new(0.4, 0, 1, 0)
AmountLabel.Text = "Jumlah Jual:"
AmountLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
AmountLabel.Font = Enum.Font.GothamMedium
AmountLabel.TextSize = 11
AmountLabel.BackgroundTransparency = 1
AmountLabel.TextXAlignment = Enum.TextXAlignment.Left

local AmountBox = Instance.new("TextBox", AmountFrame)
AmountBox.Size = UDim2.new(0.55, 0, 1, 0)
AmountBox.Position = UDim2.new(0.45, 0, 0, 0)
AmountBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
AmountBox.TextColor3 = Color3.fromRGB(0, 255, 170)
AmountBox.Text = "1" -- Default Jumlah
AmountBox.PlaceholderText = "Angka..."
AmountBox.Font = Enum.Font.GothamBold
AmountBox.TextSize = 12
Instance.new("UICorner", AmountBox).CornerRadius = UDim.new(0, 6)

-- Validasi Input (Hanya Angka)
AmountBox:GetPropertyChangedSignal("Text"):Connect(function()
    AmountBox.Text = AmountBox.Text:gsub("%D+", "")
end)
-- ------------------------------

local function AddSellButton(name, val)
    local choiceBtn = Instance.new("TextButton", SellDropdownFrame)
    choiceBtn.Size = UDim2.new(1, 0, 0, 25)
    choiceBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    choiceBtn.TextColor3 = Color3.new(0.8, 0.8, 0.8)
    choiceBtn.Text = name
    choiceBtn.Font = Enum.Font.Gotham
    choiceBtn.TextSize = 10
    Instance.new("UICorner", choiceBtn).CornerRadius = UDim.new(0, 4)
    
    choiceBtn.MouseButton1Click:Connect(function()
        SelectedSellItem = val
        SellSelectorBtn.Text = "Jual: " .. name
        SellDropdownOpen = false
        SellDropdownFrame.Visible = false
        SellDropdownFrame.Size = UDim2.new(0.9, 0, 0, 0)
    end)
    return choiceBtn
end

local function RefreshSellList()
    for _, child in pairs(SellDropdownFrame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    local foundItems = {}
    local itemCount = 0

    pcall(function()
        local gearPath = ReplicatedStorage.Modules.ModelDownloader.Collection.Gears
        if gearPath then
            for _, item in pairs(gearPath:GetChildren()) do
                if not foundItems[item.Name] then
                    AddSellButton(item.Name, item.Name)
                    foundItems[item.Name] = true
                    itemCount = itemCount + 1
                end
            end
        end
    end)

    pcall(function()
        local stonePath = Player.PlayerGui["Exclusive Store"].Main.Content.Items.Stone
        if stonePath then
            for _, item in pairs(stonePath:GetChildren()) do
                if item:IsA("Frame") or item:IsA("ImageButton") or item:IsA("TextButton") then
                    if not foundItems[item.Name] then
                        AddSellButton(item.Name, item.Name)
                        foundItems[item.Name] = true
                        itemCount = itemCount + 1
                    end
                end
            end
        end
    end)
    
    if not foundItems["Enchant Stone"] then AddSellButton("Enchant Stone (Manual)", "Enchant Stone"); itemCount=itemCount+1 end
    if not foundItems["Train Wheel"] then AddSellButton("Train Wheel (Manual)", "Train Wheel"); itemCount=itemCount+1 end
    
    AddSellButton(">> COMBO: Stone + Wheel", "Enchant Stone,Train Wheel ")
    itemCount = itemCount + 1
    
    local contentHeight = itemCount * 27
    if contentHeight > 150 then contentHeight = 150 end
    SellDropdownFrame.CanvasSize = UDim2.new(0, 0, 0, itemCount * 27)
    return contentHeight
end

SellSelectorBtn.MouseButton1Click:Connect(function()
    SellDropdownOpen = not SellDropdownOpen
    if SellDropdownOpen then
        local height = RefreshSellList() or 100
        SellDropdownFrame.Visible = true
        SellDropdownFrame:TweenSize(UDim2.new(0.9, 0, 0, height), "Out", "Quad", 0.3, true)
    else
        SellDropdownFrame:TweenSize(UDim2.new(0.9, 0, 0, 0), "Out", "Quad", 0.3, true)
        task.wait(0.3)
        SellDropdownFrame.Visible = false
    end
end)

-- // ==========================================
-- // TOMBOL FITUR
-- // ==========================================

local TotemBtn = CreateButton("Auto Totem (60m): OFF", 6)
local SellBtn = CreateButton("Auto Sell (2s): OFF", 7)
local PotatoBtn = CreateButton("Booster Mode: OFF", 8)
local WowBtn = CreateButton("WALK ON WATER: OFF", 9)
local RenderBtn = CreateButton("No Render 3D: OFF", 10)
local FpsCapBtn = CreateButton("FPS CAP: UNLIMITED", 11)

-- Footer FPS
local FpsDisplay = Instance.new("TextLabel", MainFrame)
FpsDisplay.Size = UDim2.new(1, 0, 0, 20)
FpsDisplay.Position = UDim2.new(0, 0, 1, -20)
FpsDisplay.BackgroundTransparency = 1
FpsDisplay.TextColor3 = ACCENT
FpsDisplay.Text = "FPS: --"
FpsDisplay.Font = Enum.Font.Code
FpsDisplay.TextSize = 12

-- // LOGIKA AUTO SELL (UPDATED)
SellBtn.MouseButton1Click:Connect(function()
    if not SelectedSellItem then
        game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Seraphin Helper"; Text = "Pilih Item Jual dulu!"; Duration = 3;})
        return
    end

    -- Ambil nilai jumlah dari Textbox
    local sellAmount = tonumber(AmountBox.Text)
    if not sellAmount or sellAmount <= 0 then
        sellAmount = 1 -- Default jika kosong/error
        AmountBox.Text = "1"
    end

    AutoSellEnabled = not AutoSellEnabled
    SellBtn.Text = AutoSellEnabled and "Auto Sell: ON" or "Auto Sell (2s): OFF"
    SellBtn.BackgroundColor3 = AutoSellEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)

    if AutoSellEnabled then
        SellThread = task.spawn(function()
            while AutoSellEnabled do
                pcall(function()
                    local netFolder = ReplicatedStorage.Packages._Index["sleitnick_net@0.2.0"].net
                    local sellEvent = netFolder:FindFirstChild("RF/SellItem")
                    if sellEvent then
                        -- Argument: [1] Nama Item, [2] Jumlah
                        local args = { 
                            [1] = SelectedSellItem,
                            [2] = sellAmount 
                        }
                        
                        sellEvent:InvokeServer(unpack(args))
                        print("Auto Sell: " .. SelectedSellItem .. " | Qty: " .. sellAmount)
                    end
                end)
                
                -- DELAY 2 DETIK
                task.wait(2)
            end
        end)
    else
        if SellThread then task.cancel(SellThread) end
    end
end)

-- LOGIKA AUTO TOTEM
TotemBtn.MouseButton1Click:Connect(function()
    if not SelectedTotemName then
        game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Seraphin Helper"; Text = "Pilih Totem dulu!"; Duration = 3;})
        return
    end

    AutoTotemEnabled = not AutoTotemEnabled
    TotemBtn.Text = AutoTotemEnabled and "Auto Totem: ON" or "Auto Totem: OFF"
    TotemBtn.BackgroundColor3 = AutoTotemEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)

    if AutoTotemEnabled then
        TotemThread = task.spawn(function()
            while AutoTotemEnabled do
                pcall(function()
                    local netFolder = ReplicatedStorage.Packages._Index["sleitnick_net@0.2.0"].net
                    if netFolder then
                        local args = { [1] = SelectedTotemName }
                        local spawnEvent = netFolder["RE/SpawnTotem"]
                        if spawnEvent then spawnEvent:FireServer(unpack(args)) end
                        task.wait(1.5)
                        local equipEvent = netFolder:FindFirstChild("RE/EquipItem") 
                        if equipEvent then equipEvent:FireServer(unpack(args)) end
                    end
                end)
                for i = 3600, 1, -1 do
                    if not AutoTotemEnabled then break end
                    task.wait(1)
                end
            end
        end)
    else
        if TotemThread then task.cancel(TotemThread) end
    end
end)

-- FITUR LAIN
PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "Booster Mode: ON" or "Booster Mode: OFF"
    PotatoBtn.BackgroundColor3 = Optimized and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    Lighting.GlobalShadows = not Optimized
    if Optimized then
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic; v.CastShadow = false
            elseif v:IsA("Texture") or v:IsA("Decal") then v.Transparency = 1
            elseif v:IsA("ParticleEmitter") then v.Enabled = false end
        end
    end
end)

local function getRootPart()
    return Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
end

WowBtn.MouseButton1Click:Connect(function()
    WowEnabled = not WowEnabled
    WowBtn.Text = WowEnabled and "Walk On Water: ON" or "Walk On Water: OFF"
    WowBtn.BackgroundColor3 = WowEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    if WowEnabled then
        local HRP = getRootPart()
        if HRP then
            BV = Instance.new("BodyVelocity", HRP)
            BV.Name = "SeraphinFloat"
            BV.MaxForce = Vector3.new(0, 1e6, 0)
            BV.Velocity = Vector3.zero
            Conn = RunService.Heartbeat:Connect(function()
                if HRP and HRP.Parent then HRP.AssemblyLinearVelocity = Vector3.new(HRP.AssemblyLinearVelocity.X, 0, HRP.AssemblyLinearVelocity.Z) end
            end)
        end
    else
        if Conn then Conn:Disconnect() end
        if Player.Character then
            for _, v in pairs(Player.Character:GetDescendants()) do
                if v:IsA("BodyVelocity") and v.Name == "SeraphinFloat" then v:Destroy() end
            end
        end
    end
end)

RenderBtn.MouseButton1Click:Connect(function()
    AfkModeActive = not AfkModeActive
    RunService:Set3dRenderingEnabled(not AfkModeActive)
    RenderBtn.Text = AfkModeActive and "No Render 3D: ON" or "No Render 3D: OFF"
    RenderBtn.BackgroundColor3 = AfkModeActive and Color3.fromRGB(200, 50, 50) or Color3.fromRGB(35, 35, 40)
end)

FpsCapBtn.MouseButton1Click:Connect(function()
    currentLimitIndex = currentLimitIndex + 1
    if currentLimitIndex > #fpsLimits then currentLimitIndex = 1 end
    local newLimit = fpsLimits[currentLimitIndex]
    if setfpscap then setfpscap(newLimit) end
    FpsCapBtn.Text = (newLimit > 900) and "FPS CAP: UNLIMITED" or "FPS CAP: " .. newLimit
end)

MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    if Minimized then
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, 35), "Out", "Quad", 0.3, true)
        ScrollFrame.Visible = false
        FpsDisplay.Visible = false
        MinBtn.Text = "SHOW"
    else
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
        task.wait(0.2)
        ScrollFrame.Visible = true
        FpsDisplay.Visible = true
        MinBtn.Text = "HIDE"
    end
end)

local lastT = os.clock()
local frames = 0
RunService.RenderStepped:Connect(function()
    frames = frames + 1
    if os.clock() - lastT >= 1 then
        FpsDisplay.Text = "FPS: " .. frames
        frames = 0
        lastT = os.clock()
    end
end)

game:GetService("StarterGui"):SetCore("SendNotification", {Title = "Seraphin Helper", Text = "Fitur Jumlah Jual (2s) Ready!", Duration = 5})
