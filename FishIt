--[[ 
 RakaHensem Utility V5 - Modular Optimized + Auto Fish
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Player = Players.LocalPlayer
local Char = Player.Character or Player.CharacterAdded:Wait()
local HRP = Char:WaitForChild("HumanoidRootPart")

-- Fishing Remotes (Ensure these paths match your game)
local Net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_net@0.2.0"):WaitForChild("net")
local ChargeRod = Net:WaitForChild("RF/ChargeFishingRod")
local RequestGame = Net:WaitForChild("RF/RequestFishingMinigameStarted")
local CompleteGame = Net:WaitForChild("RE/CompleteFishingMinigame")
local CancelInput = Net:WaitForChild("RF/CancelFishingInput")
local AutoState = Net:WaitForChild("RF/UpdateAutoFishingState")

-- State / Config
getgenv().fishingStart = false
local delayCharge = 1.15 -- Custom Bait Delay
local delayTime = 0.56   -- Custom Reel Delay
local RenderEnabled = true
local Optimized = false
local Minimized = false
local Enabled = false
local BV, Conn

-- UI Creation
if CoreGui:FindFirstChild("RakaBoostV") then CoreGui.RakaBoostV:Destroy() end
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "RakaBoostV"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, 160, 0, 250) -- Adjusted for new buttons
MainFrame.Position = UDim2.new(0.5, -80, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 6)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 25)
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 6)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.7, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SeraphinHelper V5"
Title.TextColor3 = Color3.fromRGB(0, 255, 150)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 10
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Scroll Frame
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, 0, 1, -65)
ScrollFrame.Position = UDim2.new(0, 0, 0, 30)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.CanvasSize = UDim2.new(0, 0, 1.5, 0)
ScrollFrame.ScrollBarThickness = 2

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 4)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function CreateButton(text)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.9, 0, 0, 25)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 9
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    return btn
end

-- Fishing Logic
local function startFishingSuperInstantLoop()
    while getgenv().fishingStart do
        pcall(function() 
            CancelInput:InvokeServer()
            task.wait(0.05)
            ChargeRod:InvokeServer()
            task.wait(0.05)
            RequestGame:InvokeServer(-1.11, 0, 1763651451)
            task.wait(delayCharge) -- Custom Bait Delay
            CompleteGame:FireServer()
            task.wait(delayTime)   -- Custom Reel Delay
        end)
        task.wait(0.1)
    end
end

-- UI Buttons
local FishBtn = CreateButton("AUTO FISH: OFF")
local BaitDelayBtn = CreateButton("BAIT DELAY: " .. delayCharge .. "s")
local ReelDelayBtn = CreateButton("REEL DELAY: " .. delayTime .. "s")
local BoostBtn = CreateButton("BOOSTER (LOW GRAPHIC)")

FishBtn.MouseButton1Click:Connect(function()
    getgenv().fishingStart = not getgenv().fishingStart
    FishBtn.Text = getgenv().fishingStart and "AUTO FISH: ON" or "AUTO FISH: OFF"
    FishBtn.BackgroundColor3 = getgenv().fishingStart and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 35)
    if getgenv().fishingStart then
        task.spawn(startFishingSuperInstantLoop)
    end
end)

BaitDelayBtn.MouseButton1Click:Connect(function()
    delayCharge = delayCharge >= 2.0 and 0.5 or delayCharge + 0.25
    BaitDelayBtn.Text = "BAIT DELAY: " .. string.format("%.2f", delayCharge) .. "s"
end)

ReelDelayBtn.MouseButton1Click:Connect(function()
    delayTime = delayTime >= 1.0 and 0.1 or delayTime + 0.1
    ReelDelayBtn.Text = "REEL DELAY: " .. string.format("%.2f", delayTime) .. "s"
end)

-- Original Booster Logic
BoostBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    BoostBtn.Text = Optimized and "BOOSTER: ACTIVE" or "BOOSTER: OFF"
    BoostBtn.BackgroundColor3 = Optimized and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 35)
    settings().Rendering.QualityLevel = Optimized and 1 or 0
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("Part") and Optimized then v.Material = Enum.Material.SmoothPlastic end
    end
end)

-- FPS Display
local FpsDisplay = Instance.new("TextLabel", MainFrame)
FpsDisplay.Size = UDim2.new(0.9, 0, 0, 25)
FpsDisplay.Position = UDim2.new(0.05, 0, 1, -30)
FpsDisplay.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
FpsDisplay.TextColor3 = Color3.fromRGB(0, 255, 255)
FpsDisplay.Text = "FPS: --"
FpsDisplay.Font = Enum.Font.Code
FpsDisplay.TextSize = 12
Instance.new("UICorner", FpsDisplay).CornerRadius = UDim.new(0, 4)

local lastT, frames = os.clock(), 0
RunService.RenderStepped:Connect(function()
    frames = frames + 1
    if os.clock() - lastT >= 1 then
        FpsDisplay.Text = "FPS: " .. frames
        frames, lastT = 0, os.clock()
    end
end)
