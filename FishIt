Berikut adalah versi final script Anda yang sudah ditambahkan fitur Notifikasi (StarterGui:SetCore) saat script pertama kali dijalankan. Notifikasi ini akan muncul di pojok kanan bawah layar sebagai konfirmasi bahwa Seraphin Helper sudah aktif.
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")
local StarterGui = game:GetService("StarterGui")

local Player = Players.LocalPlayer
local Char = Player.Character or Player.CharacterAdded:Wait()
local HRP = Char:WaitForChild("HumanoidRootPart")

-- // State
local AfkModeActive = false 
local Optimized = false
local Minimized = false
local WowEnabled = false
local BV, Conn

-- // FPS Settings
local fpsLimits = {30, 60, 144, 999}
local currentLimitIndex = 4

-- // UI Config
local UI_WIDTH = 210
local UI_HEIGHT = 250 
local ACCENT = Color3.fromRGB(0, 255, 170)

-- // FITUR NOTIFIKASI SAAT RUN
StarterGui:SetCore("SendNotification", {
    Title = "Seraphin Helper",
    Text = "Script Berhasil Dijalankan! Menunggu Data...",
    Icon = "rbxassetid://6023426926", -- Icon gear/helper
    Duration = 5
})

-- // Cleanup
if CoreGui:FindFirstChild("SeraphinHelper") then CoreGui.SeraphinHelper:Destroy() end

-- // UI Creation
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "SeraphinHelper"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.6, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">HELPER</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 45, 0, 22)
MinBtn.Position = UDim2.new(0.75, 0, 0.5, -11)
MinBtn.Text = "HIDE"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 9
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 4)

-- Scrolling Content
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -10, 1, -70)
ScrollFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 2
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 6)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function CreateButton(text)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.95, 0, 0, 35)
    btn.Text = text
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 10
    btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- // TOMBOL FITUR
local PotatoBtn = CreateButton("Booster Mode: OFF")
local WowBtn = CreateButton("WALK ON WATER: OFF")
local RenderBtn = CreateButton("No Render 3D: OFF")
local FpsCapBtn = CreateButton("FPS CAP: UNLIMITED")
local SellMenuBtn = CreateButton("SELL ITEMS: SCAN")

-- Sell Panel (Pop-out)
local SellPanel = Instance.new("Frame", MainFrame)
SellPanel.Size = UDim2.new(0, 170, 0, 180)
SellPanel.Position = UDim2.new(1.05, 0, 0, 0)
SellPanel.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
SellPanel.Visible = false
Instance.new("UICorner", SellPanel)

local SellScroll = Instance.new("ScrollingFrame", SellPanel)
SellScroll.Size = UDim2.new(1, -10, 1, -10)
SellScroll.Position = UDim2.new(0, 5, 0, 5)
SellScroll.BackgroundTransparency = 1
SellScroll.ScrollBarThickness = 2
SellScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UIListLayout", SellScroll).Padding = UDim.new(0, 4)

-- // LOGIC SELL EVERYWHERE
local function SellItem(itemName)
    print("Mencoba menjual: " .. itemName)
    -- Logic pemanggilan sistem Sell dari Fisch
end

SellMenuBtn.MouseButton1Click:Connect(function()
    SellPanel.Visible = not SellPanel.Visible
    if SellPanel.Visible then
        for _, v in pairs(SellScroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
        local items = Player.Backpack:GetChildren()
        if #items == 0 then
            SellMenuBtn.Text = "BACKPACK KOSONG"
            task.wait(1)
            SellMenuBtn.Text = "SELL ITEMS: SCAN"
        else
            for _, item in pairs(items) do
                local sBtn = Instance.new("TextButton", SellScroll)
                sBtn.Size = UDim2.new(1, 0, 0, 30)
                sBtn.Text = item.Name
                sBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
                sBtn.TextColor3 = Color3.new(1,1,1)
                sBtn.Font = Enum.Font.Gotham
                sBtn.TextSize = 10
                Instance.new("UICorner", sBtn)
                
                sBtn.MouseButton1Click:Connect(function()
                    SellItem(item.Name)
                    sBtn.Text = "TERJUAL!"
                    sBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
                    task.wait(0.5)
                    sBtn:Destroy()
                end)
            end
        end
    end
end)

-- // LOGIC FPS (STATS SERVICE)
task.spawn(function()
    while task.wait(0.5) do
        local fps = math.floor(Stats.WorkspaceResources.Heartbeat:GetValue())
        local FpsDisplay = MainFrame:FindFirstChild("FpsDisplay") or Instance.new("TextLabel", MainFrame)
        FpsDisplay.Name = "FpsDisplay"
        FpsDisplay.Size = UDim2.new(1, 0, 0, 25)
        FpsDisplay.Position = UDim2.new(0, 0, 1, -25)
        FpsDisplay.BackgroundTransparency = 1
        FpsDisplay.TextColor3 = ACCENT
        FpsDisplay.Text = "FPS: " .. fps
        FpsDisplay.Font = Enum.Font.Code
        FpsDisplay.TextSize = 12
    end
end)

-- // LOGIC BUTTONS LAINNYA
FpsCapBtn.MouseButton1Click:Connect(function()
    currentLimitIndex = currentLimitIndex + 1
    if currentLimitIndex > #fpsLimits then currentLimitIndex = 1 end
    local newLimit = fpsLimits[currentLimitIndex]
    if setfpscap then setfpscap(newLimit) end
    FpsCapBtn.Text = newLimit > 900 and "FPS CAP: UNLIMITED" or "FPS CAP: " .. newLimit
end)

PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "Booster Mode: ON" or "Booster Mode: OFF"
    PotatoBtn.BackgroundColor3 = Optimized and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    Lighting.GlobalShadows = not Optimized
    if Optimized then
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic v.CastShadow = false
            elseif v:IsA("Texture") or v:IsA("Decal") then v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then v.Enabled = false end
        end
    end
end)

RenderBtn.MouseButton1Click:Connect(function()
    AfkModeActive = not AfkModeActive
    RunService:Set3dRenderingEnabled(not AfkModeActive)
    RenderBtn.Text = AfkModeActive and "No Render 3D: ON" or "No Render 3D: OFF"
    RenderBtn.BackgroundColor3 = AfkModeActive and Color3.fromRGB(200, 50, 50) or Color3.fromRGB(35, 35, 40)
end)

WowBtn.MouseButton1Click:Connect(function()
    WowEnabled = not WowEnabled
    WowBtn.Text = WowEnabled and "Walk On Water: ON" or "Walk On Water: OFF"
    WowBtn.BackgroundColor3 = WowEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    if WowEnabled then
        BV = Instance.new("BodyVelocity", HRP)
        BV.MaxForce = Vector3.new(0, 1e6, 0)
        BV.Velocity = Vector3.zero
        Conn = RunService.Heartbeat:Connect(function()
            if HRP then HRP.AssemblyLinearVelocity = Vector3.new(HRP.AssemblyLinearVelocity.X, 0, HRP.AssemblyLinearVelocity.Z) end
        end)
    else
        if Conn then Conn:Disconnect() end
        if BV then BV:Destroy() end
    end
end)

MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    if Minimized then
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, 35), "Out", "Quad", 0.3, true)
        ScrollFrame.Visible = false SellPanel.Visible = false MinBtn.Text = "SHOW"
    else
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
        task.wait(0.2)
        ScrollFrame.Visible = true MinBtn.Text = "HIDE"
    end
end)

