local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")

local Player = Players.LocalPlayer
local Char = Player.Character or Player.CharacterAdded:Wait()
local HRP = Char:WaitForChild("HumanoidRootPart")

-- // State Variables
local Optimized = false
local Minimized = false
local InstantPromptEnabled = false
local SelectedItem = ""

-- // UI Config
local UI_WIDTH = 280
local UI_HEIGHT = 450 
local ACCENT = Color3.fromRGB(0, 255, 170)
local BG_DARK = Color3.fromRGB(15, 15, 17)
local ITEM_BG = Color3.fromRGB(25, 25, 30)

-- // Cleanup Old UI
if CoreGui:FindFirstChild("SeraphinHelper") then CoreGui.SeraphinHelper:Destroy() end

-- // Anti-AFK
Player.Idled:Connect(function()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

-- // UI Creation
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "SeraphinHelper"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.2, 0)
MainFrame.BackgroundColor3 = BG_DARK
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.8, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">MANAGER V7</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(0.95, 0.95, 0.95)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 12
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 45, 0, 22)
MinBtn.Position = UDim2.new(1, -55, 0.5, -11)
MinBtn.Text = "HIDE"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 10
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 6)

-- Main Scroll
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -10, 1, -65)
ScrollFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 2
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollFrame.ScrollBarImageColor3 = ACCENT

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 6)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Helper UI Function
local function CreateButton(text)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.98, 0, 0, 30)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    btn.BackgroundColor3 = ITEM_BG
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- --- GEARS SCANNER SECTION ---
local InvLabel = Instance.new("TextLabel", ScrollFrame)
InvLabel.Size = UDim2.new(1, 0, 0, 20)
InvLabel.BackgroundTransparency = 1
InvLabel.Text = "--- GEARS LIST ---"
InvLabel.TextColor3 = ACCENT
InvLabel.Font = Enum.Font.Code
InvLabel.TextSize = 10

local ScanBtn = CreateButton("SCAN GEARS")
ScanBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
ScanBtn.TextColor3 = ACCENT

local InvListFrame = Instance.new("ScrollingFrame", ScrollFrame)
InvListFrame.Size = UDim2.new(0.98, 0, 0, 130) 
InvListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
InvListFrame.ScrollBarThickness = 2
InvListFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UICorner", InvListFrame).CornerRadius = UDim.new(0, 6)
Instance.new("UIListLayout", InvListFrame).Padding = UDim.new(0, 2)

local SelectedDisplay = Instance.new("TextLabel", ScrollFrame)
SelectedDisplay.Size = UDim2.new(0.98, 0, 0, 25)
SelectedDisplay.BackgroundTransparency = 1
SelectedDisplay.Text = "Selected: None"
SelectedDisplay.TextColor3 = Color3.fromRGB(200, 200, 200)
SelectedDisplay.Font = Enum.Font.GothamBold
SelectedDisplay.TextSize = 11

local SellSelectBtn = CreateButton("SELL SELECTED")
SellSelectBtn.BackgroundColor3 = Color3.fromRGB(80, 40, 40)
SellSelectBtn.TextColor3 = Color3.fromRGB(255, 100, 100)

-- --- LOGIC SCANNER ---
local function UpdateGearsList()
    for _, child in pairs(InvListFrame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    local gearsPath = ReplicatedStorage:FindFirstChild("Modules")
    if gearsPath then gearsPath = gearsPath:FindFirstChild("ModelDownloader") end
    if gearsPath then gearsPath = gearsPath:FindFirstChild("Collection") end
    if gearsPath then gearsPath = gearsPath:FindFirstChild("Gears") end
    
    if gearsPath then
        for _, gear in pairs(gearsPath:GetChildren()) do
            local btn = Instance.new("TextButton", InvListFrame)
            btn.Size = UDim2.new(1, 0, 0, 25)
            btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
            btn.Text = "  " .. gear.Name
            btn.TextColor3 = Color3.new(0.9, 0.9, 0.9)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 11
            btn.TextXAlignment = Enum.TextXAlignment.Left
            btn.BorderSizePixel = 0
            
            btn.MouseButton1Click:Connect(function()
                SelectedItem = gear.Name
                SelectedDisplay.Text = "Selected: " .. gear.Name
                for _, b in pairs(InvListFrame:GetChildren()) do
                    if b:IsA("TextButton") then b.BackgroundColor3 = Color3.fromRGB(30, 30, 35) end
                end
                btn.BackgroundColor3 = ACCENT
            end)
        end
        ScanBtn.Text = "REFRESH (" .. #gearsPath:GetChildren() .. " Gears)"
    else
        ScanBtn.Text = "PATH ERROR"
    end
end

ScanBtn.MouseButton1Click:Connect(UpdateGearsList)

-- --- LOGIC SELL (DENGAN PATH TERBARU) ---
SellSelectBtn.MouseButton1Click:Connect(function()
    if SelectedItem == "" then return end
    
    local success, err = pcall(function()
        -- PATH REMOTE SESUAI PERMINTAAN TERBARU
        local remote = ReplicatedStorage.Packages._Index["sleitnick_net@0.2.0"].net["RF/SellItem"]
        
        -- Mengirim nama item terpilih
        remote:InvokeServer(SelectedItem)
    end)
    
    if success then
        SelectedDisplay.Text = "Sold: " .. SelectedItem
    else
        warn("Path/Remote Error: " .. tostring(err))
        SelectedDisplay.Text = "Error! Cek Console."
    end
end)

-- --- OTHER TOOLS ---
local PotatoBtn = CreateButton("Booster Mode: OFF")
local InstantBtn = CreateButton("Instant Interaction: OFF")

PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "Booster Mode: ON" or "Booster Mode: OFF"
    PotatoBtn.TextColor3 = Optimized and ACCENT or Color3.fromRGB(220, 220, 220)
    Lighting.GlobalShadows = not Optimized
    if Optimized then
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic elseif v:IsA("Texture") then v.Transparency = 1 end
        end
    end
end)

InstantBtn.MouseButton1Click:Connect(function()
    InstantPromptEnabled = not InstantPromptEnabled
    InstantBtn.Text = InstantPromptEnabled and "Instant Interaction: ON" or "Instant Interaction: OFF"
    InstantBtn.TextColor3 = InstantPromptEnabled and ACCENT or Color3.fromRGB(220, 220, 220)
    task.spawn(function()
        while InstantPromptEnabled do
            for _, prompt in pairs(Workspace:GetDescendants()) do
                if prompt:IsA("ProximityPrompt") then prompt.HoldDuration = 0 end
            end
            task.wait(1)
        end
    end)
end)

MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    MainFrame:TweenSize(Minimized and UDim2.new(0, UI_WIDTH, 0, 35) or UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
    ScrollFrame.Visible = not Minimized
    MinBtn.Text = Minimized and "SHOW" or "HIDE"
end)
