--[[ 
 RakaHensem Utility V12 - Totem Selector
 Update: Added Smart Scan & Single Select for Totems
 Fix: Button cycles through available Totems in Backpack
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")

local Player = Players.LocalPlayer
local function GetChar()
    return Player.Character or Player.CharacterAdded:Wait()
end

-- // State
local AfkModeActive = false 
local Optimized = false
local Minimized = false
local WowEnabled = false
local BV, Conn

-- // Totem State
local TargetTotemName = nil -- Menyimpan nama Totem yang dipilih
local TotemList = {} -- Daftar nama totem yang ditemukan
local TotemIndex = 0 -- Index pemilihan saat ini

-- // FPS Settings
local fpsLimits = {30, 60, 144, 999} 
local currentLimitIndex = 4 

-- // UI Config
local UI_WIDTH = 210
local UI_HEIGHT = 245
local ACCENT = Color3.fromRGB(0, 255, 170)

-- // Cleanup
if CoreGui:FindFirstChild("SeraphinHelper") then CoreGui.SeraphinHelper:Destroy() end

-- // UI Creation
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "SeraphinHelper"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Header
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.6, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">HELPER</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 45, 0, 22)
MinBtn.Position = UDim2.new(0.75, 0, 0.5, -11)
MinBtn.Text = "HIDE"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 9
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 4)

-- Scrolling Content
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -10, 1, -65)
ScrollFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 2
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 6)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
Layout.SortOrder = Enum.SortOrder.LayoutOrder

local function CreateButton(text, layoutOrder)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.95, 0, 0, 35)
    btn.Text = text
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 10
    btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
    btn.LayoutOrder = layoutOrder or 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- // TOMBOL FITUR
local PotatoBtn = CreateButton("POTATO MODE: OFF", 1)
local WowBtn = CreateButton("WALK ON WATER: OFF", 2)
local RenderBtn = CreateButton("AFK MODE (BLACK): OFF", 3)
local TotemBtn = CreateButton("AUTO TOTEM: OFF", 4) -- Tombol Selector
local FpsCapBtn = CreateButton("FPS CAP: UNLIMITED", 5)

-- Footer FPS
local FpsDisplay = Instance.new("TextLabel", MainFrame)
FpsDisplay.Size = UDim2.new(1, 0, 0, 20)
FpsDisplay.Position = UDim2.new(0, 0, 1, -20)
FpsDisplay.BackgroundTransparency = 1
FpsDisplay.TextColor3 = ACCENT
FpsDisplay.Text = "FPS: --"
FpsDisplay.Font = Enum.Font.Code
FpsDisplay.TextSize = 10

-- // HELPER: SCAN TOTEM UNIK
local function ScanBackpackForTotems()
    local found = {}
    local uniqueCheck = {}
    local Backpack = Player:FindFirstChild("Backpack")
    
    if Backpack then
        for _, item in pairs(Backpack:GetChildren()) do
            if item:IsA("Tool") and string.find(string.lower(item.Name), "totem") then
                if not uniqueCheck[item.Name] then
                    uniqueCheck[item.Name] = true
                    table.insert(found, item.Name)
                end
            end
        end
    end
    return found
end

-- // LOGIC: AUTO PLACE SELECTED TOTEM
local function PlaceSelectedTotem()
    if not TargetTotemName then return end
    
    local Char = GetChar()
    local Humanoid = Char:FindFirstChild("Humanoid")
    local Backpack = Player:FindFirstChild("Backpack")
    
    if not Humanoid or not Backpack then return end

    local specificTotem = nil

    -- Cari item dengan nama PERSIS sama dengan yang dipilih
    if Backpack:FindFirstChild(TargetTotemName) then
        specificTotem = Backpack[TargetTotemName]
    else
        -- Cek jika sedang dipegang
        local held = Char:FindFirstChild(TargetTotemName)
        if held then specificTotem = held end
    end

    if specificTotem then
        Humanoid:EquipTool(specificTotem)
        task.wait(0.5) 
        if specificTotem.Parent == Char then
            specificTotem:Activate()
            
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Seraphin Helper";
                Text = "Placed: " .. TargetTotemName;
                Duration = 3;
            })
        end
    else
        -- Jika Totem yang dipilih habis/hilang
        print("Totem " .. TargetTotemName .. " tidak ditemukan di tas.")
    end
end

-- Loop Eksekusi (Tetap 60 Menit)
task.spawn(function()
    while true do
        if TargetTotemName then
            pcall(PlaceSelectedTotem)
            -- Tunggu 60 menit
            for i = 1, 3600 do 
                if not TargetTotemName then break end -- Putus loop jika dimatikan
                task.wait(1)
            end
        else
            task.wait(1)
        end
    end
end)

-- // LOGIC TOMBOL TOTEM (CYCLE/SELECTOR)
TotemBtn.MouseButton1Click:Connect(function()
    -- 1. Scan dulu isi tas
    TotemList = ScanBackpackForTotems()
    
    if #TotemList == 0 then
        TotemBtn.Text = "NO TOTEM FOUND!"
        task.wait(1)
        TotemBtn.Text = "AUTO TOTEM: OFF"
        TargetTotemName = nil
        return
    end

    -- 2. Pindah ke pilihan berikutnya
    TotemIndex = TotemIndex + 1

    -- 3. Cek batas pilihan
    if TotemIndex > #TotemList then
        -- Jika sudah lewat batas list, matikan (OFF)
        TargetTotemName = nil
        TotemBtn.Text = "AUTO TOTEM: OFF"
        TotemBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
        TotemIndex = 0 -- Reset ke 0
    else
        -- Pilih Totem sesuai urutan
        TargetTotemName = TotemList[TotemIndex]
        TotemBtn.Text = "AUTO: " .. string.upper(TargetTotemName)
        TotemBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
        
        -- Notifikasi pilihan
        print("Selected Totem: " .. TargetTotemName)
        
        -- Jalankan langsung saat dipilih
        task.spawn(function() pcall(PlaceSelectedTotem) end)
    end
end)

-- // LOGIC FPS CAP (Sama seperti V10)
FpsCapBtn.MouseButton1Click:Connect(function()
    currentLimitIndex = currentLimitIndex + 1
    if currentLimitIndex > #fpsLimits then currentLimitIndex = 1 end
    local newLimit = fpsLimits[currentLimitIndex]
    if setfpscap then setfpscap(newLimit) end
    FpsCapBtn.Text = (newLimit > 900) and "FPS CAP: UNLIMITED" or "FPS CAP: " .. newLimit
end)

-- // LOGIC FITUR LAIN (Potato, Render, WoW, Minimize)
PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "POTATO MODE: ON" or "POTATO MODE: OFF"
    PotatoBtn.BackgroundColor3 = Optimized and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    Lighting.GlobalShadows = not Optimized
    if Optimized then
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic; v.CastShadow = false
            elseif v:IsA("Texture") or v:IsA("Decal") then v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then v.Enabled = false end
        end
    end
end)

RenderBtn.MouseButton1Click:Connect(function()
    AfkModeActive = not AfkModeActive
    RunService:Set3dRenderingEnabled(not AfkModeActive)
    RenderBtn.Text = AfkModeActive and "AFK MODE (BLACK): ON" or "AFK MODE (BLACK): OFF"
    RenderBtn.BackgroundColor3 = AfkModeActive and Color3.fromRGB(200, 50, 50) or Color3.fromRGB(35, 35, 40)
end)

WowBtn.MouseButton1Click:Connect(function()
    WowEnabled = not WowEnabled
    WowBtn.Text = WowEnabled and "WOW: ON" or "WOW: OFF"
    WowBtn.BackgroundColor3 = WowEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(35, 35, 40)
    if WowEnabled then
        BV = Instance.new("BodyVelocity", GetChar():WaitForChild("HumanoidRootPart"))
        BV.MaxForce = Vector3.new(0, 1e6, 0)
        BV.Velocity = Vector3.zero
        Conn = RunService.Heartbeat:Connect(function()
            local hrp = GetChar():FindFirstChild("HumanoidRootPart")
            if hrp then hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 0, hrp.AssemblyLinearVelocity.Z) end
        end)
    else
        if Conn then Conn:Disconnect() end
        if BV then BV:Destroy() end
    end
end)

MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    if Minimized then
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, 35), "Out", "Quad", 0.3, true)
        ScrollFrame.Visible = false
        FpsDisplay.Visible = false
        MinBtn.Text = "SHOW"
    else
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
        task.wait(0.2)
        ScrollFrame.Visible = true
        FpsDisplay.Visible = true
        MinBtn.Text = "HIDE"
    end
end)

local lastT = os.clock(); local frames = 0
RunService.RenderStepped:Connect(function()
    frames = frames + 1
    if os.clock() - lastT >= 1 then
        FpsDisplay.Text = "FPS: " .. frames
        frames = 0
        lastT = os.clock()
    end
end)
