local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local MaterialService = game:GetService("MaterialService")

local Player = Players.LocalPlayer
local Char = Player.Character or Player.CharacterAdded:Wait()
local HRP = Char:WaitForChild("HumanoidRootPart")

-- // State
local AfkModeActive = false 
local Optimized = false
local Minimized = false
local WowEnabled = false
local IsFrozen = false
local BV, Conn

-- // Config
local UI_WIDTH = 230
local UI_HEIGHT = 220
local ACCENT = Color3.fromRGB(0, 255, 170)
local BG_COLOR = Color3.fromRGB(15, 15, 17)

-- // Cleanup
if CoreGui:FindFirstChild("SeraphinHelper") then CoreGui.SeraphinHelper:Destroy() end

-- // Notification System
local function Notify(title, msg)
    local NotifGui = Instance.new("ScreenGui", CoreGui)
    local Frame = Instance.new("Frame", NotifGui)
    Frame.Size = UDim2.new(0, 200, 0, 60)
    Frame.Position = UDim2.new(1, 10, 0.9, 0)
    Frame.BackgroundColor3 = BG_COLOR
    Frame.BorderSizePixel = 0
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local Stroke = Instance.new("UIStroke", Frame)
    Stroke.Color = ACCENT
    Stroke.Thickness = 1.5

    local T = Instance.new("TextLabel", Frame)
    T.Size = UDim2.new(1, 0, 0.4, 0)
    T.Text = title:upper()
    T.TextColor3 = ACCENT
    T.Font = Enum.Font.GothamBold
    T.TextSize = 12
    T.BackgroundTransparency = 1

    local M = Instance.new("TextLabel", Frame)
    M.Size = UDim2.new(1, 0, 0.6, 0)
    M.Position = UDim2.new(0, 0, 0.4, 0)
    M.Text = msg
    M.TextColor3 = Color3.new(1,1,1)
    M.Font = Enum.Font.Gotham
    M.TextSize = 10
    M.BackgroundTransparency = 1

    Frame:TweenPosition(UDim2.new(1, -210, 0.9, 0), "Out", "Quad", 0.5)
    task.delay(3, function()
        Frame:TweenPosition(UDim2.new(1, 10, 0.9, 0), "In", "Quad", 0.5)
        task.wait(0.5)
        NotifGui:Destroy()
    end)
end

-- // UI Creation
local Gui = Instance.new("ScreenGui", CoreGui)
Gui.Name = "SeraphinHelper"

local MainFrame = Instance.new("Frame", Gui)
MainFrame.Size = UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -UI_WIDTH/2, 0.3, 0)
MainFrame.BackgroundColor3 = BG_COLOR
MainFrame.BackgroundTransparency = 0.1
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
Header.BackgroundTransparency = 0.2
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0.55, 0, 1, 0)
Title.Position = UDim2.new(0.05, 0, 0, 0)
Title.Text = "SERAPHIN <font color=\"rgb(0,255,170)\">HELPER</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

local FpsDisplay = Instance.new("TextLabel", Header)
FpsDisplay.Size = UDim2.new(0.25, 0, 1, 0)
FpsDisplay.Position = UDim2.new(0.58, 0, 0, 0)
FpsDisplay.BackgroundTransparency = 1
FpsDisplay.TextColor3 = ACCENT
FpsDisplay.Text = "60 FPS"
FpsDisplay.Font = Enum.Font.GothamBold
FpsDisplay.TextSize = 11
FpsDisplay.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 40, 0, 22)
MinBtn.Position = UDim2.new(0.82, 0, 0.5, -11)
MinBtn.Text = "HIDE"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 8
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 4)

local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -14, 1, -50)
ScrollFrame.Position = UDim2.new(0, 7, 0, 45)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 0
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y

local Layout = Instance.new("UIListLayout", ScrollFrame)
Layout.Padding = UDim.new(0, 6)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function CreateButton(text)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(0.98, 0, 0, 30)
    btn.Text = text
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 10
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    btn.BackgroundTransparency = 0.3
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- // Fitur
local FreezeBtn = CreateButton("Freeze Player: OFF")
local WowBtn = CreateButton("Walk On Water: OFF")
local PotatoBtn = CreateButton("Booster Mode: OFF")
local RenderBtn = CreateButton("No Render 3D: OFF")
local FpsCapBtn = CreateButton("FPS CAP: UNLIMITED")

local function UpdateBtnVisual(btn, state)
    if state then
        btn.BackgroundColor3 = ACCENT
        btn.TextColor3 = Color3.new(0, 0, 0)
        btn.BackgroundTransparency = 0
    else
        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        btn.BackgroundTransparency = 0.3
    end
end

-- // Boost Logic (Enhanced)
PotatoBtn.MouseButton1Click:Connect(function()
    Optimized = not Optimized
    PotatoBtn.Text = Optimized and "Booster Mode: ON" or "Booster Mode: OFF"
    UpdateBtnVisual(PotatoBtn, Optimized)
    
    -- Lighting Optimization
    Lighting.GlobalShadows = not Optimized
    Lighting.Mode = Optimized and Enum.LightingMode.Compatibility or Enum.LightingMode.Voxel
    
    for _, v in pairs(Lighting:GetChildren()) do
        if v:IsA("PostEffect") or v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = not Optimized
        end
    end

    -- Material & World Optimization
    for _, v in pairs(game:GetDescendants()) do
        if Optimized then
            if v:IsA("BasePart") and v.Material ~= Enum.Material.SmoothPlastic then
                v.Material = Enum.Material.SmoothPlastic
                v.CastShadow = false
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            end
        else
            if v:IsA("BasePart") then
                v.CastShadow = true
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v.Transparency = 0
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = true
            end
        end
    end
    
    -- ReplicatedStorage Cleanup (NPC & VFX)
    local vfx = ReplicatedStorage:FindFirstChild("VFX")
    local npc = ReplicatedStorage:FindFirstChild("NPC")
    if vfx then vfx.Parent = Optimized and nil or ReplicatedStorage end
    if npc then npc.Parent = Optimized and nil or ReplicatedStorage end
end)

-- // Fitur Lainnya (Freeze, WoW, Render, dll)
FreezeBtn.MouseButton1Click:Connect(function()
    IsFrozen = not IsFrozen
    if HRP then HRP.Anchored = IsFrozen end
    FreezeBtn.Text = IsFrozen and "Freeze Player: ON" or "Freeze Player: OFF"
    UpdateBtnVisual(FreezeBtn, IsFrozen)
end)

WowBtn.MouseButton1Click:Connect(function()
    WowEnabled = not WowEnabled
    WowBtn.Text = WowEnabled and "Walk On Water: ON" or "Walk On Water: OFF"
    UpdateBtnVisual(WowBtn, WowEnabled)
    if WowEnabled then
        BV = Instance.new("BodyVelocity", HRP)
        BV.MaxForce = Vector3.new(0, 1e6, 0)
        BV.Velocity = Vector3.zero
        Conn = RunService.Heartbeat:Connect(function()
            if HRP then HRP.AssemblyLinearVelocity = Vector3.new(HRP.AssemblyLinearVelocity.X, 0, HRP.AssemblyLinearVelocity.Z) end
        end)
    else
        if Conn then Conn:Disconnect() end
        if BV then BV:Destroy() end
    end
end)

RenderBtn.MouseButton1Click:Connect(function()
    AfkModeActive = not AfkModeActive
    RunService:Set3dRenderingEnabled(not AfkModeActive)
    RenderBtn.Text = AfkModeActive and "No Render 3D: ON" or "No Render 3D: OFF"
    UpdateBtnVisual(RenderBtn, AfkModeActive)
end)

FpsCapBtn.MouseButton1Click:Connect(function()
    local limits = {30, 60, 144, 999}
    currentLimitIndex = (currentLimitIndex or 4) % #limits + 1
    local newLimit = limits[currentLimitIndex]
    if setfpscap then setfpscap(newLimit) end
    FpsCapBtn.Text = (newLimit > 900) and "FPS CAP: UNLIMITED" or "FPS CAP: " .. newLimit
end)

MinBtn.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    if Minimized then
        ScrollFrame.Visible = false
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, 40), "Out", "Quad", 0.3, true)
        MainFrame.BackgroundTransparency = 1
        Header.BackgroundTransparency = 1
        MinBtn.Text = "SHOW"
    else
        MainFrame:TweenSize(UDim2.new(0, UI_WIDTH, 0, UI_HEIGHT), "Out", "Quad", 0.3, true)
        task.wait(0.2)
        ScrollFrame.Visible = true
        MainFrame.BackgroundTransparency = 0.1
        Header.BackgroundTransparency = 0.2
        MinBtn.Text = "HIDE"
    end
end)

local lastT, frames = os.clock(), 0
RunService.RenderStepped:Connect(function()
    frames = frames + 1
    if os.clock() - lastT >= 1 then
        FpsDisplay.Text = frames .. " FPS"
        frames, lastT = 0, os.clock()
    end
end)

Notify("Seraphin Helper", "Script Berhasil Dijalankan!")
